# 🚀 Быстрый старт

## ✅ Исправления применены!

Проблемы с белой страницей исправлены:

- ✅ GetStorage инициализируется первым
- ✅ Добавлена обработка ошибок
- ✅ Улучшен роутинг с fallback UI

## 📝 Как запустить с ngrok

### 1. Запустите бэкенд

```bash
cd ../alien_tap_backend
cargo run
```

Должен запуститься на `http://localhost:8000`

### 2. Запустите ngrok (в другом терминале)

```bash
ngrok http 8000
```

Скопируйте HTTPS URL (например: `https://unhedonistically-nonpragmatical-deonna.ngrok-free.dev`)

### 3. Запустите Flutter с ngrok URL

```bash
cd alien_tap
flutter run -d chrome --dart-define=API_BASE_URL=https://unhedonistically-nonpragmatical-deonna.ngrok-free.dev
```

**Или используйте скрипт:**

```bash
./scripts/run_with_ngrok.sh
```

## 🔍 Проверка

1. **Откройте консоль браузера** (F12 или Cmd+Option+I)
2. **Проверьте логи:**

   - Должны быть: `✅ GetStorage initialized`
   - Должны быть: `🔧 AppConfig: API Base URL: ...`
   - Должны быть: `✅ App scope setup completed`
   - Должны быть: `🔍 Router redirect: ...`

3. **Если видите ошибки:**
   - Скопируйте текст ошибки
   - Проверьте, что бэкенд запущен
   - Проверьте, что ngrok активен

## 🐛 Если все еще белая страница

1. **Очистите кеш:**

   ```bash
   flutter clean
   flutter pub get
   ```

2. **Перезапустите:**

   ```bash
   flutter run -d chrome --dart-define=API_BASE_URL=https://your-ngrok-url.ngrok-free.dev
   ```

3. **Проверьте консоль браузера** на ошибки JavaScript

## 📱 Тестирование в Telegram

1. Убедитесь, что Flutter запущен с ngrok URL
2. Откройте Telegram бота
3. Запустите WebApp
4. Должен открыться экран авторизации

---

**Удачи! 🚀**
