# 🚀 Задеплой бэкенда (рекомендуется)

## ✅ Преимущества

- ✅ Бэкенд доступен по постоянному URL
- ✅ Не нужно два ngrok туннеля
- ✅ Фронтенд работает локально через ngrok (для Telegram WebApp)
- ✅ Быстро и просто

## 📋 Пошаговая инструкция

### Шаг 1: Задеплойте бэкенд на Railway

1. **Создайте аккаунт:** https://railway.app
2. **Создайте проект:**
   - New Project → Deploy from GitHub repo
   - Выберите `alien-tap-backend`
3. **Добавьте PostgreSQL:**
   - New → Database → PostgreSQL
   - Railway автоматически создаст `DATABASE_URL`
4. **Настройте переменные окружения:**
   ```
   TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
   JWT_SECRET=<сгенерируйте: openssl rand -base64 32>
   DEV_MODE=false
   PORT=8000
   DATABASE_URL=<автоматически из PostgreSQL>
   ```
5. **Получите URL бэкенда:**
   - Railway даст URL типа: `https://alien-tap-backend-production.up.railway.app`
   - Сохраните этот URL!

### Шаг 2: Пересоберите фронтенд с production URL

```bash
cd "/Users/anymacstore/Flutter development/alien_tap"
flutter build web --dart-define=API_BASE_URL=https://your-backend-url.railway.app
```

### Шаг 3: Запустите фронтенд локально через ngrok

```bash
cd build/web
python3 -m http.server 8080
```

В другом терминале:

```bash
ngrok http 8080
```

Получите URL: `https://frontend-xyz789.ngrok-free.dev`

### Шаг 4: Настройте Telegram бота

Используйте URL фронтенда: `https://frontend-xyz789.ngrok-free.dev`

---

## 🎯 Итоговая схема

```
Бэкенд:  Railway (production)  →  https://backend.railway.app (постоянный URL)
Фронтенд: localhost:8080  →  ngrok  →  https://frontend-xyz.ngrok-free.dev (для Telegram)

Фронтенд использует: API_BASE_URL=https://backend.railway.app
Telegram бот использует: https://frontend-xyz.ngrok-free.dev
```

---

## ✅ Преимущества этого подхода

1. **Бэкенд всегда доступен** - не нужно перезапускать ngrok
2. **Один ngrok туннель** - только для фронтенда
3. **Постоянный URL бэкенда** - не меняется
4. **Быстро** - деплой бэкенда занимает ~5 минут

---

## 🔄 Альтернатива: Задеплоить фронтенд

Если хотите задеплоить фронтенд вместо бэкенда:

1. **Задеплойте фронтенд на Vercel:**

   - Подключите GitHub репозиторий
   - Build command: `flutter build web --dart-define=API_BASE_URL=https://your-ngrok-backend-url.ngrok-free.dev`
   - Output directory: `build/web`

2. **Бэкенд локально через ngrok:**

   ```bash
   ngrok http 8000
   ```

3. **Настройте Telegram бота:**
   - Используйте URL Vercel: `https://your-app.vercel.app`

Но это менее удобно, так как ngrok URL бэкенда будет меняться.

---

**Рекомендация: Задеплойте бэкенд на Railway! 🚀**
