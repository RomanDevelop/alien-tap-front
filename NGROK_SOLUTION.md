# ✅ Решение проблемы с двумя ngrok туннелями

## 🚫 Проблема

На бесплатном плане ngrok не позволяет использовать один домен для двух туннелей одновременно.

## ✅ Решение: Используйте один ngrok для фронтенда

### Схема:

```
Бэкенд:  localhost:8000  →  ngrok туннель 1  →  https://backend-url.ngrok-free.dev
Фронтенд: localhost:8080  →  ngrok туннель 2  →  https://frontend-url.ngrok-free.dev
```

**НО:** На бесплатном плане нужно запускать их в разных терминалах с разными портами API (4040 и 4041).

## 🚀 Правильная настройка

### Шаг 1: Запустите бэкенд ngrok (Терминал 1)

```bash
ngrok http 8000
```

Запишите URL: `https://backend-abc123.ngrok-free.dev`

### Шаг 2: Запустите фронтенд ngrok с другим портом API (Терминал 2)

```bash
ngrok http 8080 --web-addr=:4041
```

Или просто в другом терминале (ngrok автоматически использует другой порт):

```bash
ngrok http 8080
```

Запишите URL: `https://frontend-xyz789.ngrok-free.dev`

### Шаг 3: Пересоберите фронтенд с URL бэкенда

```bash
cd alien_tap
flutter build web --dart-define=API_BASE_URL=https://backend-abc123.ngrok-free.dev
cd build/web
python3 -m http.server 8080
```

### Шаг 4: Настройте Telegram бота

Используйте URL фронтенда: `https://frontend-xyz789.ngrok-free.dev`

---

## 🔄 Альтернатива: Используйте один ngrok с проксированием

Если не получается запустить два ngrok, можно использовать один ngrok для фронтенда и настроить проксирование запросов к бэкенду через фронтенд.

Но это сложнее и требует дополнительной настройки.

---

## ✅ Рекомендуемое решение

**Используйте два терминала:**

1. **Терминал 1:** `ngrok http 8000` (бэкенд)
2. **Терминал 2:** `ngrok http 8080` (фронтенд)

ngrok автоматически использует разные порты для API (4040 и 4041), поэтому они не будут конфликтовать.

---

**Удачи! 🚀**
