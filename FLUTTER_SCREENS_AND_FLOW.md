# 📱 Рекомендации по экранам и логике переходов для Flutter приложения

## 🎯 Основная идея UI/UX

**Проблема текущего UI:** Баннер авторизации на основном экране создаёт путаницу. Пользователь видит игру, но не может играть.

**Решение:** Разделить на отдельные экраны с чёткой логикой переходов.

---

## 📺 Необходимые экраны

### 1. **Экран авторизации (AuthScreen)**

**Когда показывается:**

- При первом запуске приложения
- Если токен отсутствует или истёк
- При ошибке 401 Unauthorized

**Содержимое:**

- Логотип/иконка игры
- Заголовок "Авторизация через Telegram"
- Кнопка "Авторизоваться" (большая, заметная)
- Индикатор загрузки при процессе авторизации
- Обработка ошибок (если авторизация не удалась)

**Логика:**

- При нажатии на кнопку → вызов `POST /auth/telegram`
- При успехе → сохранение токена → переход на `GameScreen`
- При ошибке → показать сообщение об ошибке, возможность повторить

---

### 2. **Основной игровой экран (GameScreen)**

**Когда показывается:**

- После успешной авторизации
- Основной экран приложения

**Содержимое:**

- **Верхняя панель:**
  - Текущий счёт пользователя (крупно)
  - Возможно, аватар/имя пользователя (если есть)
- **Центральная область:**
  - Большая кнопка "TAP" (основное действие)
  - Анимация при нажатии
  - Визуальная обратная связь (увеличение, эффекты)
- **Нижняя панель:**
  - Кнопка "Лидерборд" → переход на `LeaderboardScreen`
  - Кнопка "Забрать награду" → переход на `ClaimScreen`
  - Кнопка "Выход" (опционально, в меню)

**Логика:**

- При каждом нажатии TAP → увеличить локальный счёт
- Периодически (или при достижении определённого значения) → отправлять `POST /game/update_score` на сервер
- Можно отправлять каждые N очков (например, каждые 10) или при паузе в игре
- При ошибке 401 → переход на `AuthScreen` для переавторизации

---

### 3. **Экран лидерборда (LeaderboardScreen)**

**Когда показывается:**

- По нажатию кнопки "Лидерборд" с `GameScreen`

**Содержимое:**

- Заголовок "Топ игроков"
- Кнопка обновления (обновить список)
- Список игроков:
  - Место (1, 2, 3...)
  - Имя пользователя (username или first_name)
  - Счёт
  - Выделение текущего пользователя (если он в списке)
- Кнопка "Назад" → возврат на `GameScreen`

**Логика:**

- При открытии → загрузка `GET /game/leaderboard`
- Кнопка обновления → повторный запрос
- Если пользователь не в топ-10 → показать его позицию отдельно (например, "Ваше место: 25")

---

### 4. **Экран вывода токенов (ClaimScreen)**

**Когда показывается:**

- По нажатию кнопки "Забрать награду" с `GameScreen`

**Содержимое:**

- Заголовок "Забрать награду"
- Информация о текущем счёте пользователя
- Поле ввода суммы (или выбор из доступных сумм)
- Кнопка "Забрать"
- Индикатор загрузки
- Кнопка "Назад" → возврат на `GameScreen`

**Логика (двухэтапный процесс):**

1. **Начало вывода:**

   - Пользователь вводит сумму
   - Нажатие "Забрать" → `POST /claim/start` с `amount`
   - Получение `claim_id`
   - Показ подтверждения или переход на следующий шаг

2. **Подтверждение:**
   - Экран подтверждения с информацией о транзакции
   - Кнопка "Подтвердить" → `POST /claim/confirm` с `claim_id`
   - При успехе → показать сообщение "Вывод успешно выполнен"
   - Возврат на `GameScreen`

**Обработка ошибок:**

- Если 401 → переход на `AuthScreen`
- Если недостаточно средств → показать сообщение
- Если claim уже обработан → показать соответствующее сообщение

---

## 🔄 Логика переходов (Flow)

### Основной flow:

```
1. Запуск приложения
   ↓
2. Проверка токена (из SharedPreferences)
   ↓
   ├─ Токен есть и валидный? → GameScreen
   └─ Токена нет или невалидный? → AuthScreen

3. AuthScreen
   ↓
   Пользователь нажимает "Авторизоваться"
   ↓
   POST /auth/telegram
   ↓
   ├─ Успех → Сохранить токен → GameScreen
   └─ Ошибка → Показать ошибку, возможность повторить

4. GameScreen (основной экран)
   ↓
   ├─ Нажатие TAP → Увеличить счёт → Периодически отправлять на сервер
   ├─ Кнопка "Лидерборд" → LeaderboardScreen
   └─ Кнопка "Забрать награду" → ClaimScreen

5. LeaderboardScreen
   ↓
   Загрузка GET /game/leaderboard
   ↓
   Кнопка "Назад" → GameScreen

6. ClaimScreen
   ↓
   Ввод суммы → POST /claim/start
   ↓
   Экран подтверждения → POST /claim/confirm
   ↓
   Успех → GameScreen
```

---

## 🛠️ Рекомендуемые модули для реализации

**Зачем:** Управление переходами между экранами, deep linking, защита маршрутов (требование авторизации)

**Использование:**

- Определить маршруты: `/auth`, `/game`, `/leaderboard`, `/claim`
- Guard для `/game`, `/leaderboard`, `/claim` → проверка токена, редирект на `/auth` если нет

---

**Зачем:** Управление состоянием авторизации, счёта, лидерборда

**Структура провайдеров:**

- `AuthProvider` - состояние авторизации (токен, userId, isAuthenticated)
- `GameProvider` - состояние игры (локальный счёт, синхронизация с сервером)
- `LeaderboardProvider` - состояние лидерборда (список игроков, загрузка)
- `ClaimProvider` - состояние вывода (текущий claim, процесс)

---

### 3. **HTTP клиент**

```yaml
dio: ^5.4.0
```

**Зачем:** Работа с API, interceptors для автоматической подстановки токена

**Interceptors:**

- Добавлять `Authorization: Bearer <token>` к каждому запросу
- При 401 → очистить токен → редирект на AuthScreen
- Retry логика для сетевых ошибок

---

### 4. **Локальное хранилище**

```yaml
secure storage
```

**Зачем:** Сохранение JWT токена, userId, настроек

---

### 5. **Telegram WebApp SDK**

```yaml
telegram_web_app: ^0.1.0 # Или аналогичный пакет
```

**Зачем:** Получение `initData` для авторизации

---

---

## 📋 Детальная логика каждого экрана

### AuthScreen

**Состояние:**

```dart
enum AuthState { idle, loading, success, error }
```

**Логика:**

1. При инициализации → проверить, есть ли токен
2. Если есть → автоматически попробовать валидировать (опционально)
3. Кнопка "Авторизоваться":
   - Получить `initData` из Telegram WebApp SDK
   - Установить состояние `loading`
   - Отправить `POST /auth/telegram`
   - При успехе:
     - Сохранить токен в SharedPreferences
     - Обновить AuthProvider
     - Навигация на `/game`
   - При ошибке:
     - Показать сообщение об ошибке
     - Установить состояние `error`
     - Возможность повторить

---

### GameScreen

**Состояние:**

```dart
int localScore = 0;
int serverScore = 0;  // Последний сохранённый на сервере
bool isSyncing = false;
```

**Логика TAP:**

1. Увеличить `localScore++`
2. Обновить UI
3. Если `localScore % 10 == 0` (или другая логика) → синхронизировать с сервером
4. Синхронизация:
   - `POST /game/update_score` с `score: localScore`
   - При успехе → обновить `serverScore = localScore`
   - При 401 → переход на AuthScreen
   - При ошибке → показать snackbar, но продолжить игру

**Логика при открытии:**

- Загрузить текущий счёт пользователя (можно из локального хранилища или запросить с сервера)
- Инициализировать `localScore` и `serverScore`

**Логика при закрытии/паузе:**

- Отправить финальный счёт на сервер (если `localScore > serverScore`)

---

### LeaderboardScreen

**Состояние:**

```dart
List<LeaderboardEntry> leaderboard = [];
bool isLoading = false;
String? currentUserId;
```

**Логика:**

1. При открытии → `GET /game/leaderboard`
2. Сохранить `currentUserId` из AuthProvider
3. Отобразить список с выделением текущего пользователя
4. Кнопка обновления → повторный запрос
5. Pull-to-refresh (опционально) → обновление списка

**UI:**

- Если список пуст → показать "Пока нет игроков"
- Если загрузка → показать shimmer/skeleton
- Если ошибка → показать сообщение и кнопку повторить

---

### ClaimScreen

**Состояние:**

```dart
double amount = 0.0;
String? claimId;
ClaimState state = ClaimState.input;  // input, processing, confirmation, success, error
```

**Логика (двухэтапная):**

**Этап 1 - Ввод суммы:**

1. Пользователь вводит сумму (валидация: > 0, <= доступный баланс)
2. Кнопка "Забрать" → `POST /claim/start`
3. При успехе:
   - Сохранить `claimId`
   - Переход к этапу 2 (экран подтверждения)

**Этап 2 - Подтверждение:**

1. Показать информацию о транзакции (сумма, claim_id)
2. Кнопка "Подтвердить" → `POST /claim/confirm`
3. При успехе:
   - Показать сообщение "Вывод успешно выполнен"
   - Через 2-3 секунды → возврат на GameScreen
4. При ошибке:
   - Показать сообщение об ошибке
   - Возможность вернуться и повторить

**Альтернатива (один экран с модальным окном):**

- Основной экран с вводом суммы
- При нажатии "Забрать" → показать модальное окно подтверждения
- В модальном окне → кнопка "Подтвердить" → `POST /claim/confirm`

---

## 🔐 Логика авторизации и защиты маршрутов

### Guard для защищённых маршрутов:

```dart
// Псевдокод для go_router
final router = GoRouter(
  redirect: (context, state) {
    final isAuthenticated = context.read<AuthProvider>().isAuthenticated;
    final isAuthRoute = state.matchedLocation == '/auth';

    // Если не авторизован и не на экране авторизации → редирект на /auth
    if (!isAuthenticated && !isAuthRoute) {
      return '/auth';
    }

    // Если авторизован и на экране авторизации → редирект на /game
    if (isAuthenticated && isAuthRoute) {
      return '/game';
    }

    return null; // Разрешить доступ
  },
  routes: [
    GoRoute(path: '/auth', builder: (context, state) => AuthScreen()),
    GoRoute(path: '/game', builder: (context, state) => GameScreen()),
    GoRoute(path: '/leaderboard', builder: (context, state) => LeaderboardScreen()),
    GoRoute(path: '/claim', builder: (context, state) => ClaimScreen()),
  ],
);
```

---

## 🎨 Рекомендации по UI/UX

### 1. **Переходы между экранами**

- Использовать плавные анимации (FadeTransition, SlideTransition)
- При авторизации → показать индикатор загрузки, затем плавный переход

### 2. **Обратная связь**

- При каждом TAP → визуальная/тактильная обратная связь
- При синхронизации счёта → показать индикатор (небольшой, ненавязчивый)
- При ошибках → показывать понятные сообщения (не технические)

### 3. **Состояния загрузки**

- Все экраны должны показывать состояние загрузки
- Использовать skeleton loading для лидерборда
- Disable кнопки во время загрузки

### 4. **Обработка ошибок**

- Сетевые ошибки → показать сообщение с возможностью повторить
- 401 ошибка → автоматический переход на AuthScreen
- Валидация на клиенте перед отправкой запросов

---

## 📊 Схема состояний приложения

```
AppState
├── AuthState
│   ├── isAuthenticated: bool
│   ├── token: String?
│   ├── userId: String?
│   └── isLoading: bool
│
├── GameState
│   ├── localScore: int
│   ├── serverScore: int
│   ├── isSyncing: bool
│   └── lastSyncTime: DateTime?
│
├── LeaderboardState
│   ├── entries: List<LeaderboardEntry>
│   ├── isLoading: bool
│   └── error: String?
│
└── ClaimState
    ├── amount: double
    ├── claimId: String?
    ├── status: ClaimStatus
    └── error: String?
```

---

## ✅ Чек-лист реализации

### Экраны:

- [ ] AuthScreen - экран авторизации
- [ ] GameScreen - основной игровой экран
- [ ] LeaderboardScreen - таблица лидеров
- [ ] ClaimScreen - экран вывода токенов (двухэтапный процесс)

### Логика:

- [ ] Проверка токена при запуске
- [ ] Автоматический редирект на AuthScreen если нет токена
- [ ] Guard для защищённых маршрутов
- [ ] Синхронизация счёта с сервером (периодическая)
- [ ] Обработка 401 ошибки (автоматическая переавторизация)
- [ ] Сохранение состояния при закрытии приложения

### UI/UX:

- [ ] Плавные переходы между экранами
- [ ] Индикаторы загрузки
- [ ] Обработка ошибок с понятными сообщениями
- [ ] Визуальная обратная связь при действиях
- [ ] Pull-to-refresh для лидерборда

---

## 🚀 Итоговая рекомендация

**Главное изменение:** Убрать баннер авторизации с основного экрана. Вместо этого:

1. **При запуске** → проверить токен → если нет → показать `AuthScreen`
2. **После авторизации** → переход на `GameScreen` (чистый, без баннеров)
3. **Все защищённые экраны** → автоматическая проверка токена, редирект на авторизацию при необходимости

Это создаст более понятный и профессиональный UX, где пользователь чётко понимает, что нужно сделать для начала игры.
