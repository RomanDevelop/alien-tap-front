# 🚀 Быстрый деплой на Vercel (решение проблемы)

## ❌ Проблема

Vercel не поддерживает Flutter - команда `flutter` не найдена.

## ✅ Решение: Собрать локально и задеплоить

### Шаг 1: Соберите проект локально

```bash
cd "/Users/anymacstore/Flutter development/alien_tap"
flutter build web --dart-define=API_BASE_URL=https://unhedonistically-nonpragmatical-deonna.ngrok-free.dev
```

### Шаг 2: Установите Vercel CLI (если еще не установлен)

```bash
npm i -g vercel
```

### Шаг 3: Задеплойте собранные файлы

```bash
cd build/web
vercel --prod
```

**Или используйте скрипт:**
```bash
./scripts/deploy_to_vercel.sh
```

---

## 🔄 Альтернатива: Отключить автоматический деплой в Vercel

1. **В Vercel Dashboard:**
   - Settings → Git
   - Отключите автоматический деплой (или удалите интеграцию с GitHub)

2. **Используйте только Vercel CLI для деплоя:**
   ```bash
   ./scripts/deploy_to_vercel.sh
   ```

---

## 📋 Что происходит

1. ✅ Flutter собирает проект локально (у вас есть Flutter SDK)
2. ✅ Создается папка `build/web` со статическими файлами
3. ✅ Vercel CLI загружает эти файлы на Vercel
4. ✅ Vercel просто раздает статические файлы (без сборки)

---

## ⚠️ Важно

- При каждом изменении нужно пересобирать и задеплоить
- ngrok URL меняется при перезапуске - обновите `API_BASE_URL`

**Лучше задеплойте бэкенд на Railway!** См. [DEPLOY_BACKEND_FIRST.md](DEPLOY_BACKEND_FIRST.md)

